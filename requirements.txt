# Core Deep Learning Framework
torch>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<2.0.0  # For statistical significance testing

# LLM Integration
transformers>=4.40.0,<5.0.0
accelerate>=0.28.0,<1.0.0
sentencepiece>=0.2.0,<1.0.0
protobuf>=3.20.0,<4.0

# Quantization (8-bit loading for memory efficiency)
bitsandbytes>=0.41.0  # For 8-bit quantization

# Configuration Management
pyyaml>=6.0,<7.0

# Testing Framework
pytest>=7.4.0,<8.0.0
pytest-cov>=4.1.0,<5.0.0

# Data Processing
datasets>=2.14.0,<3.0.0
pandas>=2.0.0,<3.0.0

# Visualization & Analysis
matplotlib>=3.7.0,<4.0.0
seaborn>=0.12.0,<1.0.0
jupyter>=1.0.0,<2.0.0
ipywidgets>=8.0.0,<9.0.0

# Progress Bars
tqdm>=4.65.0,<5.0.0

# Logging
loguru>=0.7.0,<1.0.0

# Type Checking (development)
mypy>=1.4.0,<2.0.0

# Table Formatting (for publication-ready tables)
tabulate>=0.9.0,<1.0.0  # For markdown/LaTeX table generation

# Optional: GPU Monitoring (useful for resource tracking)
# pynvml>=11.5.0  # Uncomment for GPU monitoring

# Optional: Weights & Biases (for experiment tracking)
# wandb>=0.15.0  # Uncomment for W&B integration
